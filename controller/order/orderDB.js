const {createError}= require('../../utils')
const Order = require("../../models/Order");
const Cart = require("../../models/Cart");
const Billing = require("../../models/Billing");

// module scaffholding 
const  orderDB = {}

//  create order
orderDB.create =async () => {
    try {
    
    } catch (error) {
        throw error 
    }
};
module.exports = orderDB