const { createError } = require("../../utils");

// module scaffholding
const orderService = {};

//  create order
orderService.create = async (userId,productIds) => {
	try {

	} catch (error) {
		throw error;
	}
};
module.exports = orderService;
